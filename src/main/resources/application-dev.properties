spring.datasource.url=jdbc:postgresql://localhost:${POSTGRES_DB_PORT_EXTERNAL}/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.ai.openai.enabled=true
# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.default_schema=public
spring.jpa.show-sql=true

# MongoDB
spring.data.mongodb.host=${MONGO_DB_HOST}
spring.data.mongodb.port=${MONGO_DB_PORT_EXTERNAL}
spring.data.mongodb.database=${MONGO_DB_NAME}
spring.data.mongodb.auto-index-creation=true
spring.data.mongodb.auto-create-indexes=true



# Payments configuration (placeholders)
payments.mercadopago.checkoutBase=${MP_CHECKOUT_BASE:https://www.mercadopago.com/checkout}

# MercadoPago real integration
payments.mercadopago.accessToken=${MP_ACCESS_TOKEN:APP_USR-8952257273892467-102222-c5d7bd775e8e198732d1beda16a7836d-2941946163}
payments.webhook.mercadopago=${MP_WEBHOOK_URL:https://unscandalous-lynsey-supervoluminously.ngrok-free.dev/api/payments/webhook/mercadopago}

# Payment flow URLs and currency
payments.successUrl=${PAYMENTS_SUCCESS_URL:http://localhost:5173/payments/success}
payments.cancelUrl=${PAYMENTS_CANCEL_URL:http://localhost:5173/payments/cancel}
payments.currency=${PAYMENTS_CURRENCY:ARS}

# Default pricing (override as needed)
payments.pricing.chapter=${PAYMENTS_PRICE_CHAPTER:500.0}
payments.pricing.author=${PAYMENTS_PRICE_AUTHOR:1500.0}
payments.pricing.work.fallback=${PAYMENTS_PRICE_WORK_FALLBACK:2000.0}
